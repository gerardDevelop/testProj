class MyCharacterProps {
	constructor() {
		this.movingDirection = 'none';
		
	}

	update(delta) {
		// player specific updates, like movement on screen, as well as packaging this new movement to be sent as a position update to server
				
	}
}

export default MyCharacterProps;